apiVersion: v1
kind: Service
metadata:
  name: {{ include "justera.fullname" . }}
  labels:
    {{- include "justera.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "justera.selectorLabels" . | nindent 4 }}
