apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "justera.fullname" . }}-test-connection"
  labels:
    {{- include "justera.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "justera.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
